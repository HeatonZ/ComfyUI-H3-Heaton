# Example workflows

JSON workflow templates are planned for a later revision. For now, connect the
loader -> H3 conditioning/sampler path, always keep video and audio VAE decode
paths paired, and use MC LoadLatent for continuation segments.
