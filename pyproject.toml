[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "comfyui-h3-heaton"
version = "0.1.0"
description = "MiniMax H3 ComfyUI nodes with a canonical AV bundle and compatibility adapters"
requires-python = ">=3.13"
dependencies = []

[tool.comfy]
PublisherId = "h3-heaton"
DisplayName = "H3 Heaton"
Icon = ""
